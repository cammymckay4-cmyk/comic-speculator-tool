# GoCollect API Key for comic book market data
# Copy this file to .env and replace with your actual API key

# Server-side API key (secure - not exposed to client)
GOCOLLECT_API_KEY=your_gocollect_api_key_here

# Client-side API key (exposed in browser - use for testing/demo only)
VITE_GOCOLLECT_API_KEY=demo_key_for_testing

# Vercel Environment Variable Setup:
# To deploy this application on Vercel, you need to add these environment variables:
# 1. Go to your Vercel project dashboard
# 2. Navigate to Settings > Environment Variables
# 3. Add these variables:
#    - Name: GOCOLLECT_API_KEY (server-side secure)
#      Value: [your actual API key]
#      Environment: All environments (Production, Preview, Development)
#    - Name: VITE_GOCOLLECT_API_KEY (client-side for testing)
#      Value: [demo key or public key if available]
#      Environment: All environments (Production, Preview, Development)
# 4. Redeploy your application to apply the new environment variables